name: Build and Deploy Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Репозиторыг татаж авах
      - name: Checkout repository
        uses: actions/checkout@v2

      # 2. Docker image үүсгэх
      - name: Build Docker image
        run: |
          docker build -t admin673/rampage2247:latest .

      # 3. Docker Hub руу нэвтрэх
      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login --username "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # 4. Docker Hub руу image push хийх
      - name: Push Docker image to Docker Hub
        run: |
          docker push admin673/rampage2247:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # 1. Railway CLI суулгах
      - name: Install Railway CLI
        run: |
          curl -sSL https://railway.app/install.sh | sh

      # 2. Railway API түлхүүрийг зөв хадгалах
      - name: Set Railway API key
        run: |
          mkdir -p ~/.railway
          echo "${{ secrets.RAILWAY_API_KEY }}" > ~/.railway/apiKey

      # 3. Railway project-тэй холбоо барих
      - name: Link to Railway project
        run: |
          railway link -p 0ff3de39-7a77-40b8-9365-4ff1ce9c2ce3

      # 4. Railway руу deploy хийх
      - name: Deploy to Railway
        run: |
          railway up --image admin673/rampage2247:latest
          
